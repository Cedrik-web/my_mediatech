{% extends 'base.html.twig' %}

{% block title %}ForWhy{% endblock %}

{% block body %}
    <h1>ForWhy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ for_why.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ for_why.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_for_why_index') }}">back to list</a>

    <a href="{{ path('app_for_why_edit', {'id': for_why.id}) }}">edit</a>

    {{ include('for_why/_delete_form.html.twig') }}
{% endblock %}
