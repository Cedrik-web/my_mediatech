{% extends 'base.html.twig' %}

{% block title %}Network{% endblock %}

{% block body %}
    <h1>Network</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ network.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ network.createdAt ? network.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ network.updatedAt ? network.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_network_index') }}">back to list</a>

    <a href="{{ path('app_network_edit', {'id': network.id}) }}">edit</a>

    {{ include('network/_delete_form.html.twig') }}
{% endblock %}
